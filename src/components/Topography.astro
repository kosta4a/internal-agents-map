---
// ABOUTME: Draws the contour map that sits behind a page header, as authored art.
// ABOUTME: The header that holds it needs `position: relative` and `isolation: isolate`.
import topography from '../assets/topography.svg?raw';
---
<Fragment set:html={topography} />
