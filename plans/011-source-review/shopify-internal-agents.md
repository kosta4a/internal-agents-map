# Shopify Aquifer / River

## Captures read

- `shopify-internal-agents-source-1` ("Under the River", 180 lines) in full.

## Disposition decisions

- Purpose: `summary` (Aquifer as platform; River plus PR-review and Vanilla
  profiles; requested research, migration, and compliance-scan agents).
- Workflow: the article documents exactly one representative run — the
  "when a River thread goes well" sequence at lines 73–79. Appended as
  `primitives.3` (public @river request, lines 69, 75), `.4` (work with
  partial findings, lines 65, 76), `.5` (human redirect absorbed mid-thread,
  lines 77–78), and `.6` (searchable transcript feeding skills, lines 71, 79).
  Scope names one River thread; PR review and Vanilla stay platform-wide
  capability, per the Cloudflare platform pattern. The three pre-existing
  primitives are mechanisms (architecture split, public-by-default property,
  skills-as-files).
- Human involvement: reported from the redirect step only (`primitives.5`).
  No required checkpoint on River's own output is documented; the catalog
  judgment on the unknown attention boundary stays in research details, and
  PR review is described as often having no human (line 147).
- Implementation: all eight fields have claims. `model` reports the swap
  design; no specific model is named (line 127). `harness`/`sandbox` follow
  lines 120–122; `tool_access` line 65 plus the gateway (116); `knowledge`
  lines 24, 87; `context_mgmt` lines 38, 87, 134.
- Validation: `unreported`. Tests River runs are part of its work and the CI
  improvements serve human developers (lines 33, 47); no check of River's own
  output is documented.
- Observations: all four canonical. `key_metrics.1` is NOT a duplicate of the
  headline — it adds the 3,536 absolute merged-PR count to the one-in-eight
  share (lines 12, 83); same reasoning as Cloudflare's request/token metric.
  Session and channel counts (83) and the two medians (65) have distinct
  subjects.
- Lessons: all four supported at their cited locators.

## Flags

1. `architecture.sandbox` credits the brain-and-hands framing to Anthropic;
   the capture references only an unnamed linked essay at line 108. Claim
   unchanged; the field note states the boundary.
2. `architecture.credentials` lists Shopify SSO; no capture passage names
   SSO. Claim unchanged; the field note states the boundary.
3. `architecture.interfaces` lists github; the capture documents pull-request
   creation but never names the host. Claim unchanged; the field note states
   the boundary.

## Deliberate exclusions

- The 7,000+ people touched (line 83) — same passage as the session counts,
  left unclaimed with the metric subjects as recorded.
- The three consumer modes' automation and job variants (lines 147–148) —
  platform-wide capability, not the representative workflow.
- Tobi's teaching-workshop framing (14) — external post, not preserved.

## Challenge pass

- Checked that the workflow steps do not import the platform's durability
  story: session survival is implementation, not a run step.
- Checked `key_metrics.1` against the duplicate criterion both ways: it adds
  the absolute count, so it stays canonical.
- Checked that "runs tests" (65) stays inside the work step rather than
  becoming a validation disposition.

Self-check: keyscan clean; 7 questions + 8 fields exact; roles cover all 7
primitives; workflow claim paths equal the workflow-role set; 14 reported,
1 unreported, no not-reviewed.
