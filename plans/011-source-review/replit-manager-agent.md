## Replit Manager agent

### Captures read

- `replit-manager-agent-source-1` ("The Self-Driving Company"), all 184 lines.
  The record's single source; every existing claim was checked against it.

### Claim audit

- All pre-existing claims verify at their cited locators: the manager-agent
  passage (lines 76–78), the 2.9x consistent-cohort figure (44), flat review
  latency / reversion / incident trends (50–56), the sandbox stack (34), the
  Slack interface (120), and both operating models (16, 50).
- `architecture.model` remains "Not specified"; the capture names no model. The
  placeholder stays in research details with an `unreported` field state.

### Disposition decisions

- Purpose: `summary` (30–31). Workflow: appended `primitives.1` (take an
  objective and gather context, lines 16, 34) and `primitives.2` (spawn parallel
  loop agents, line 78); the pre-existing manager-agent primitive stays a
  mechanism. Scope: a person's objective through parallel loop agents to
  verifiable work product.
- Human involvement: both documented boundaries — work-product review
  (`operating_models.0`) and the risk-gated second reviewer in the agent review
  lane (`operating_models.1`, line 50).
- Validation: appended `primitives.3` (result-checking with escalation for
  judgment, line 16) carries the validation role. The agentic co-reviewer that
  catches bugs in human reviews (line 62) belongs to the review lane, not a
  separate validation claim.
- Implementation: appended `architecture.knowledge` (knowledge base plus
  codebase state and the semantic layer over the warehouse, lines 120, 132) and
  `architecture.credentials` (token proxies, line 34) at exactly the capture's
  level of detail; with harness, sandbox, tool access, context management, and
  interfaces, seven of eight fields report. Model is the only `unreported`.
- Observations: the headline is compound (2.9x output plus held quality trends).
  `key_metrics.0` and `.1` are confirmed component aliases — each restates one
  half with the same period and scope already carried by the headline metadata.
  Appended `key_metrics.2` (30% of human PR review time saved, line 50) and
  `key_metrics.3` (hardest support tickets closed 60% faster, line 168) as
  canonical observations of the review and support lanes, each self-reported
  without a described method.

### Deliberate exclusions

- The 5.8x raw lines-of-code increase (38, 44) stays in the headline's
  `measurement_method`; it is the company-wide figure the cohort metric exists
  to remove.
- Build-vs-buy cost comparisons (10x cost of vendor tools, line 106): vendor
  evaluations, not observations of this system's work.
- The self-improving continual-learning system (line 94) is the AI team's
  separate system for improving Replit Agent.
- Per-engineer anecdotes (CSS migration, PSC bug, line 84) and per-team
  workflows (sales slides, marketing specs, line 144–160) are uses, not runs of
  a documented workflow.

### Flags

- None. No contradiction between any claim and the capture.
